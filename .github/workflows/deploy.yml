name: Auto Deploy Frontend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy ke Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 34.50.122.42
          username: vargoalfonso
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            cd ~
            ./deploy.sh
